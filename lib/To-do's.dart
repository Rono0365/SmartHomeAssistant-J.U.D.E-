import 'package:flutter/material.dart';

class ScanProduct extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Make shit happen"),
    );
  }
}
